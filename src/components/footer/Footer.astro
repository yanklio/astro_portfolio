<div class="footer-wrapper">
    <footer>
        <div class="footer-left">
            <span>
                /* Made with love to the software engineering, to creating and
                experimenting with new things. To endless "puzzle". And thanks
                to a "few" cups of coffee with some snacks during this long
                journey */
            </span>
            <br />
            <br />
            <span> @2026 by yanklio </span>
        </div>
        <div class="footer-right">
            <pre
                class="ascii-coffee">   )  (
    (   ) )
     ) ( (
 mrf_______)_
 .-'---------|
( C|/\/\/\/\/|
 '-./\/\/\/\/|
   '_________'
    '-------'</pre>
        </div>
    </footer>
</div>

<style>
    .footer-wrapper {
        width: 100%;
        background: var(--bg);
        border-top: 4px dashed var(--fg3);
    }

    footer {
        width: 100%;
        padding: 26px 16px;
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: center;
        align-items: center;
        gap: 24px clamp(12px, 3vw, 32px);
        overflow: hidden;
    }

    .footer-left {
        flex: 1 1 420px;
        max-width: min(800px, 100%);
        text-align: center;
        line-height: 1.6;
        font-family: "JetBrains Mono", monospace;
        min-width: 0;
    }

    .footer-right {
        flex: 0 0 auto;
        display: flex;
        justify-content: center;
    }

    .ascii-coffee {
        color: var(--fg2);
        font-family: "JetBrains Mono", monospace;
        font-size: 0.75rem;
        font-weight: 700;
        line-height: 1.2;
        margin: 0;
    }

    span {
        display: block;
        color: var(--fg3);
        font-size: 0.95rem;
        overflow-wrap: break-word;
    }

    @media (max-width: 700px) {
        footer {
            flex-direction: column;
        }

        .footer-left {
            flex-basis: auto;
        }

        .footer-right {
            width: 100%;
        }
    }
</style>

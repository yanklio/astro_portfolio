---
import type { CollectionEntry } from "astro:content";
import BlogCard from "./BlogCard.astro";

const { posts } = Astro.props;
---

<div class="blog-list">
    {posts.map((post: CollectionEntry<"blog">) => <BlogCard post={post} />)}
</div>

<style>
    .blog-list {
        display: flex;
        flex-direction: column;
        gap: 2.5rem;
        width: 100%;
        max-width: 1080px;
        margin: 0 auto;
        padding-top: 4rem;
        padding-bottom: 8rem;
    }

</style>

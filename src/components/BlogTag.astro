---
interface Props {
    text: string;
}

const { text } = Astro.props;
---

<span class="tag">#{text}</span>

<style>
    .tag {
        color: var(--fg3);
        font-size: var(--text-meta);
        font-weight: var(--weight-bold);
    }
</style>
